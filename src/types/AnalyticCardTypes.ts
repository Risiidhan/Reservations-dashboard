import { ReactNode } from "react"

export type AnalyticCardComponentType = {
    heading: string,
    value: string,
    icon: ReactNode,
    footerTitle: string,
    footerIcon: ReactNode,
    bottomBorder: string,
}